{
  "name": "test-sandbox",
  "version": "1.0.0",
  "description": "Test app for Knight smoke tests",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "lint-staged": {
    "*.js": [
      "node --check"
    ]
  },
  "devDependencies": {
    "husky": "^4.3.8",
    "lint-staged": "^16.4.0"
  }
}
